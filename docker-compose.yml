# Esboço inicial (Fase 0): apenas o serviço `db`.
# Os serviços `server` e `client` entram na Fase 5 (orquestração completa).
services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    ports:
      # Porta publicada no HOST é configurável (evita conflito com outro Postgres
      # local). Default 5432. A porta interna do container segue sempre 5432.
      - '${POSTGRES_HOST_PORT:-5432}:5432'
    volumes:
      - db_data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}']
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  db_data:
