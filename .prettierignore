# Dependências e artefatos de build
node_modules
dist
dist-ssr
build

# Lockfiles (gerenciados pelo npm)
package-lock.json

# Prisma (client gerado + migrations SQL)
**/node_modules/.prisma
**/prisma/migrations

# OpenAPI gerado por `npm run openapi:generate` (JSON.stringify, não Prettier).
# Sem isto, formatá-lo aqui e regenerá-lo depois deixaria o CI vermelho em loop.
server/openapi.json

# Relatórios do Playwright
test-results
playwright-report

# Ambiente
.env
*.local
